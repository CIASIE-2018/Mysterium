<div class="swiper-container">
    <div class="swiper-wrapper">
    {% if not findScenario %}
        {% for card in cards %}
            <div data-idcard="{{ card }}" class="swiper-slide">
                <img src="/images/cards/{{ card }}.png">
            </div>
        {% endfor %}
    {% endif %}
    </div>
</div>